version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL="postgresql://postgres:postgres@db:5432/shopper?schema=backend"
      - AWS_S3_BUCKET_NAME='teste-tecnico-shopper'
      - AWS_ACCESS_KEY_ID='AKIAYS2NQ5DZHMIKOQPV'
      - AWS_SECRET_ACCESS_KEY='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'
    volumes:
      - ./:/usr/src/app
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=app_db
    volumes:
      - ./docker/init:/docker-entrypoint-initdb.d
    ports:
      - "5433:5432"
